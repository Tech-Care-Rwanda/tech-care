services:
  tech-care-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./logs:/app/logs