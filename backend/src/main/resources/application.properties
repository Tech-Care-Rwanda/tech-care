# App info
spring.application.name=TechCare-Rwanda
server.port=5001

# ? Local database config
spring.datasource.url=jdbc:postgresql://db.rgkddhzqjokftkkwexdd.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Prince@12002Mugisha
spring.datasource.driver-class-name=org.postgresql.Driver

# ? Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# ? Additional Database Configuration for Stability
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# ? Connection Pool (HikariCP) - Fixed for Supabase pooler
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000
spring.datasource.hikari.pool-name=TechCareHikariPool

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tech.carerwanda01@gmail.com
spring.mail.password=whpy cnph lxmy vmqk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB
spring.servlet.multipart.file-size-threshold=2KB

# Custom File Storage Paths (used by your own code, not Spring Boot by default)
file.upload.dir=${user.home}/techcare-uploads
file.upload.images.path=${file.upload.dir}/images
file.upload.documents.path=${file.upload.dir}/documents
